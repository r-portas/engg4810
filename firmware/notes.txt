# Notes to maintain my sanity and loose control

variables to control the sd card state/ writting
int sd_state
int sd_count


Button/ Enter Press in Measurement state // toggles the sd card reading (if set high)
										//  Print ask sample count
										//  if sample count selected by user (up / down) and enter
										//  if (sd_count != 0) and (sd_state == 1) {
										    write to the sd card
											samples_written ++;
										    
										    }
										    
										    if (samples_written == sd_count) || (another enter) {
										    	// sd_state toggle
										    	// print cancelled  
										    }
										
										
										